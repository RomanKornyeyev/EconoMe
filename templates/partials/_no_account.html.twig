{#
  Estado vacío global: el usuario no tiene ninguna cuenta.
  Parámetro opcional:
    message → texto explicativo bajo el título (tiene un valor por defecto).
#}
<div class="text-center py-5">
    <i class="fa-solid fa-wallet fa-3x text-muted mb-3"></i>
    <h5 class="text-muted">No tienes ninguna cuenta existente</h5>
    <p class="text-muted">{{ message|default('Crea una cuenta para empezar.') }}</p>
    <a href="{{ path('account_create') }}" class="btn btn-primary mt-2">
        <i class="fa-solid fa-plus me-1"></i>Crear cuenta
    </a>
</div>
